# Grafana 기동 시 Prometheus 데이터소스를 자동 등록한다(수동 설정 불필요).
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    # 컨테이너끼리는 호스트가 아니라 compose 서비스 이름으로 통신한다.
    url: http://prometheus:9090
    isDefault: true
