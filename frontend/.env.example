VITE_BACKEND_URL="http://localhost:8080"

# 배포 시 프론트와 백엔드가 다른 origin이면 직접 지정하세요.
# 비워두면 현재 origin의 /api, /ws 경로를 사용합니다.
VITE_API_BASE_URL=""
VITE_WS_URL=""

# OAuth 진입 베이스. dev는 백엔드 오리진 직행(프록시 우회), prod는 same-origin(빈값)
VITE_OAUTH_BASE="http://localhost:8080"
