# Presence (온라인 상태) Implementation Plan

> **실행 방식:** 이 프로젝트는 hands-on 학습 스타일 — **재민 님이 각 태스크를 직접 구현**하고, 매 태스크 후 Claude가 리뷰합니다. 아래 코드는 정답이 아니라 "이 방향으로 짜면 된다"는 참고 구현입니다. 체크박스(`- [ ]`)로 진행 추적.

**Goal:** 채팅 참가자의 실시간 온라인/오프라인 상태를 참가자 명단에 표시한다(온라인 점 + "온라인 N명" 카운트).

**Architecture:** 백엔드가 STOMP 세션 생명주기 이벤트(connect/disconnect/subscribe)를 in-memory로 추적해, 온라인 memberId 전체 스냅샷을 `/sub/presence` 전역 토픽으로 `SimpMessagingTemplate` 직접 방송한다(Redis 미경유). 프론트는 STOMP 클라를 다중 구독 라우팅으로 확장해 presence 토픽을 채팅과 별도 구독하고, 받은 온라인 집합을 참가자 명단에 반영한다.

**Tech Stack:** Spring Boot(STOMP, `@EventListener`, `SimpMessagingTemplate`), JUnit5 + AssertJ, React + TypeScript, 손으로 짠 raw-WebSocket STOMP 클라이언트.

## Global Constraints

- DB 스키마 변경 없음(in-memory). `ddl-auto: validate` 무관, 마이그레이션 불필요.
- 로스터 페이로드는 **memberId만**: `{ "onlineMemberIds": [1, 5, 8] }`.
- 온라인 = memberId가 살아있는 세션을 1개 이상 보유(탭 여러 개여도 1명; 마지막 세션 종료 시 오프라인).
- 프론트 id는 문자열로 통일(`String(memberId)`).
- 기존 typing UI / `Presence` 타입 / 메시지·Redis 파이프라인은 **손대지 않는다**.
- 백엔드 신규 클래스 패키지: `presence` (신규), DTO는 기존 `dto`.
- 프론트 검증: `npm --prefix frontend run lint`(tsc) + `vite build`. 백엔드 검증: `./gradlew build`.

---

### Task 1: PresenceRegistry (온라인 세션 추적, 순수 로직)

세션↔멤버 추적 in-memory 저장소. 순수 로직이라 TDD 유닛테스트로 잡는다.

**Files:**
- Create: `src/main/java/com/example/springboot_realtimechat/presence/PresenceRegistry.java`
- Test: `src/test/java/com/example/springboot_realtimechat/presence/PresenceRegistryTest.java`

**Interfaces:**
- Produces:
  - `void connect(String sessionId, Long memberId)`
  - `Optional<Long> disconnect(String sessionId)` — 제거된 memberId(없으면 empty)
  - `Set<Long> getOnlineMemberIds()` — 현재 온라인 memberId distinct 집합

- [ ] **Step 1: 실패하는 테스트 작성**

`src/test/java/com/example/springboot_realtimechat/presence/PresenceRegistryTest.java`:
```java
package com.example.springboot_realtimechat.presence;

import org.junit.jupiter.api.Test;

import static org.assertj.core.api.Assertions.assertThat;

class PresenceRegistryTest {

    @Test
    void 접속하면_온라인_목록에_포함된다() {
        PresenceRegistry registry = new PresenceRegistry();

        registry.connect("session-1", 10L);

        assertThat(registry.getOnlineMemberIds()).containsExactly(10L);
    }

    @Test
    void 같은_멤버가_여러_세션이면_한_명으로_집계된다() {
        PresenceRegistry registry = new PresenceRegistry();

        registry.connect("session-1", 10L);
        registry.connect("session-2", 10L);

        assertThat(registry.getOnlineMemberIds()).containsExactly(10L);
    }

    @Test
    void 마지막_세션이_끊겨야_오프라인이_된다() {
        PresenceRegistry registry = new PresenceRegistry();
        registry.connect("session-1", 10L);
        registry.connect("session-2", 10L);

        registry.disconnect("session-1");
        assertThat(registry.getOnlineMemberIds()).containsExactly(10L);

        registry.disconnect("session-2");
        assertThat(registry.getOnlineMemberIds()).isEmpty();
    }

    @Test
    void 끊긴_세션의_memberId를_반환한다() {
        PresenceRegistry registry = new PresenceRegistry();
        registry.connect("session-1", 10L);

        assertThat(registry.disconnect("session-1")).contains(10L);
        assertThat(registry.disconnect("unknown-session")).isEmpty();
    }
}
```

- [ ] **Step 2: 테스트가 실패(컴파일 에러)하는지 확인**

Run: `./gradlew test --tests "*PresenceRegistryTest"`
Expected: FAIL — `PresenceRegistry` 심볼 없음(컴파일 에러).

- [ ] **Step 3: 최소 구현**

`src/main/java/com/example/springboot_realtimechat/presence/PresenceRegistry.java`:
```java
package com.example.springboot_realtimechat.presence;

import org.springframework.stereotype.Component;

import java.util.HashSet;
import java.util.Optional;
import java.util.Set;
import java.util.concurrent.ConcurrentHashMap;

@Component
public class PresenceRegistry {

    private final ConcurrentHashMap<String, Long> sessions = new ConcurrentHashMap<>();

    public void connect(String sessionId, Long memberId) {
        sessions.put(sessionId, memberId);
    }

    public Optional<Long> disconnect(String sessionId) {
        return Optional.ofNullable(sessions.remove(sessionId));
    }

    public Set<Long> getOnlineMemberIds() {
        return new HashSet<>(sessions.values());
    }
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `./gradlew test --tests "*PresenceRegistryTest"`
Expected: PASS (4개 통과).

- [ ] **Step 5: 커밋**

```bash
git add src/main/java/com/example/springboot_realtimechat/presence/PresenceRegistry.java \
        src/test/java/com/example/springboot_realtimechat/presence/PresenceRegistryTest.java
git commit -m "feat(presence): 온라인 세션 추적 PresenceRegistry + 유닛테스트"
```

---

### Task 2: PresenceResponse DTO + WebSocketEventListener (세션 이벤트 → 방송)

STOMP 세션 이벤트를 받아 registry를 갱신하고 로스터를 방송한다. 이벤트 리스너는 STOMP 런타임 통합이라 유닛테스트 대신 **컴파일 검증(`./gradlew build`) + Task 6 E2E**로 확인한다(브리틀한 목 테스트를 만들지 않음).

**Files:**
- Create: `src/main/java/com/example/springboot_realtimechat/dto/PresenceResponse.java`
- Create: `src/main/java/com/example/springboot_realtimechat/presence/WebSocketEventListener.java`

**Interfaces:**
- Consumes: `PresenceRegistry.connect/disconnect/getOnlineMemberIds` (Task 1), `SimpMessagingTemplate`, `CustomUserDetails.getMemberId()` (기존 `security` 패키지).
- Produces: `/sub/presence` 토픽으로 `PresenceResponse { List<Long> onlineMemberIds }` JSON 방송.

- [ ] **Step 1: PresenceResponse DTO 작성**

`src/main/java/com/example/springboot_realtimechat/dto/PresenceResponse.java`:
```java
package com.example.springboot_realtimechat.dto;

import lombok.Getter;

import java.util.List;

@Getter
public class PresenceResponse {
    private final List<Long> onlineMemberIds;

    public PresenceResponse(List<Long> onlineMemberIds) {
        this.onlineMemberIds = onlineMemberIds;
    }
}
```

- [ ] **Step 2: WebSocketEventListener 작성**

`src/main/java/com/example/springboot_realtimechat/presence/WebSocketEventListener.java`:
```java
package com.example.springboot_realtimechat.presence;

import com.example.springboot_realtimechat.dto.PresenceResponse;
import com.example.springboot_realtimechat.security.CustomUserDetails;
import lombok.RequiredArgsConstructor;
import org.springframework.context.event.EventListener;
import org.springframework.messaging.simp.SimpMessagingTemplate;
import org.springframework.messaging.simp.stomp.StompHeaderAccessor;
import org.springframework.security.core.Authentication;
import org.springframework.stereotype.Component;
import org.springframework.web.socket.messaging.SessionConnectedEvent;
import org.springframework.web.socket.messaging.SessionDisconnectEvent;
import org.springframework.web.socket.messaging.SessionSubscribeEvent;

import java.security.Principal;
import java.util.ArrayList;

@Component
@RequiredArgsConstructor
public class WebSocketEventListener {

    private static final String PRESENCE_DESTINATION = "/sub/presence";

    private final PresenceRegistry presenceRegistry;
    private final SimpMessagingTemplate messagingTemplate;

    @EventListener
    public void onConnected(SessionConnectedEvent event) {
        StompHeaderAccessor accessor = StompHeaderAccessor.wrap(event.getMessage());
        String sessionId = accessor.getSessionId();
        Long memberId = extractMemberId(event.getUser());

        if (sessionId == null || memberId == null) {
            return; // 인증 안 된 연결은 추적하지 않음
        }

        presenceRegistry.connect(sessionId, memberId);
        broadcastRoster();
    }

    @EventListener
    public void onDisconnected(SessionDisconnectEvent event) {
        presenceRegistry.disconnect(event.getSessionId());
        broadcastRoster();
    }

    @EventListener
    public void onSubscribe(SessionSubscribeEvent event) {
        StompHeaderAccessor accessor = StompHeaderAccessor.wrap(event.getMessage());
        if (PRESENCE_DESTINATION.equals(accessor.getDestination())) {
            broadcastRoster(); // 방금 접속한 클라의 late-join 동기화
        }
    }

    private Long extractMemberId(Principal principal) {
        if (principal instanceof Authentication authentication
                && authentication.getPrincipal() instanceof CustomUserDetails userDetails) {
            return userDetails.getMemberId();
        }
        return null;
    }

    private void broadcastRoster() {
        PresenceResponse roster =
                new PresenceResponse(new ArrayList<>(presenceRegistry.getOnlineMemberIds()));
        messagingTemplate.convertAndSend(PRESENCE_DESTINATION, roster);
    }
}
```

- [ ] **Step 3: 컴파일 + 기존 테스트 통과 확인**

Run: `./gradlew build`
Expected: BUILD SUCCESSFUL (Task 1 테스트 포함 전체 통과, 컴파일 에러 없음).

> 참고: `event.getUser()`는 `WebSocketConfig`의 CONNECT 인터셉터가 `setUser`한 `UsernamePasswordAuthenticationToken` → `.getPrincipal()`이 `CustomUserDetails`. 방송 동작 자체는 Task 6 E2E에서 확인.

- [ ] **Step 4: 커밋**

```bash
git add src/main/java/com/example/springboot_realtimechat/dto/PresenceResponse.java \
        src/main/java/com/example/springboot_realtimechat/presence/WebSocketEventListener.java
git commit -m "feat(presence): STOMP 세션 이벤트로 온라인 로스터 방송"
```

---

### Task 3: 프론트 stomp.ts — 다중 구독 라우팅 리팩터

STOMP 클라가 채팅 구독과 presence 구독을 구분해 라우팅하도록 확장한다. `onPresence`는 **선택 옵션**으로 추가해 이 태스크 단독으로도 tsc가 통과하게 한다(App 배선은 Task 4).

**Files:**
- Modify: `frontend/src/lib/stomp.ts` (전체 교체)

**Interfaces:**
- Produces: `StompClientOptions.onPresence?: (onlineMemberIds: string[]) => void`. CONNECTED 직후 `/sub/presence` 자동 구독. presence 구독 id로 오는 MESSAGE → `onPresence(payload.onlineMemberIds.map(String))`, 채팅 구독 id로 오는 MESSAGE → 기존 `onMessage`.
- Consumes(later): Task 4 App이 `onPresence` 제공.

- [ ] **Step 1: stomp.ts 전체 교체**

`frontend/src/lib/stomp.ts`:
```ts
import { BackendMessage } from './api';

interface StompFrame {
  command: string;
  headers: Record<string, string>;
  body: string;
}

interface StompClientOptions {
  token: string;
  onConnect: () => void;
  onMessage: (message: BackendMessage) => void;
  onPresence?: (onlineMemberIds: string[]) => void;
  onDisconnect: () => void;
  onError: () => void;
}

const PRESENCE_DESTINATION = '/sub/presence';

export class SpringStompClient {
  private socket: WebSocket | null = null;
  private connected = false;
  private subscriptionId = 0;
  private currentChatSubscription?: string;
  private presenceSubscription?: string;
  // subscription id -> 종류: 들어온 MESSAGE 프레임을 알맞은 핸들러로 라우팅
  private subscriptionKinds = new Map<string, 'chat' | 'presence'>();
  private options: StompClientOptions;

  constructor(options: StompClientOptions) {
    this.options = options;
  }

  connect() {
    const configuredUrl = import.meta.env.VITE_WS_URL as string | undefined;
    const protocol = window.location.protocol === 'https:' ? 'wss:' : 'ws:';
    const url = configuredUrl || `${protocol}//${window.location.host}/ws`;

    this.socket = new WebSocket(url);
    this.socket.onopen = () => {
      this.write('CONNECT', {
        'accept-version': '1.2',
        'heart-beat': '10000,10000',
        Authorization: `Bearer ${this.options.token}`,
      });
    };

    this.socket.onmessage = (event) => this.handleRawMessage(String(event.data));
    this.socket.onclose = () => {
      this.connected = false;
      this.options.onDisconnect();
    };
    this.socket.onerror = () => this.options.onError();
  }

  disconnect() {
    if (this.connected) {
      this.write('DISCONNECT', {});
    }
    this.socket?.close();
    this.socket = null;
    this.connected = false;
    this.currentChatSubscription = undefined;
    this.presenceSubscription = undefined;
    this.subscriptionKinds.clear();
  }

  subscribe(chatroomId: string) {
    if (!this.connected) return;
    if (this.currentChatSubscription) {
      this.write('UNSUBSCRIBE', { id: this.currentChatSubscription });
      this.subscriptionKinds.delete(this.currentChatSubscription);
    }

    this.currentChatSubscription = `sub-${++this.subscriptionId}`;
    this.subscriptionKinds.set(this.currentChatSubscription, 'chat');
    this.write('SUBSCRIBE', {
      id: this.currentChatSubscription,
      destination: `/sub/chatrooms/${chatroomId}`,
      ack: 'auto',
    });
  }

  private subscribePresence() {
    if (!this.connected || this.presenceSubscription) return;
    this.presenceSubscription = `sub-${++this.subscriptionId}`;
    this.subscriptionKinds.set(this.presenceSubscription, 'presence');
    this.write('SUBSCRIBE', {
      id: this.presenceSubscription,
      destination: PRESENCE_DESTINATION,
      ack: 'auto',
    });
  }

  send(chatroomId: string, content: string) {
    if (!this.connected) return false;
    this.write('SEND', {
      destination: `/pub/chatrooms/${chatroomId}/messages`,
      'content-type': 'application/json',
    }, JSON.stringify({ content }));
    return true;
  }

  private handleRawMessage(raw: string) {
    parseFrames(raw).forEach((frame) => {
      if (frame.command === 'CONNECTED') {
        this.connected = true;
        this.subscribePresence();
        this.options.onConnect();
        return;
      }

      if (frame.command === 'MESSAGE' && frame.body) {
        const kind = this.subscriptionKinds.get(frame.headers.subscription);
        if (kind === 'presence') {
          const payload = JSON.parse(frame.body) as { onlineMemberIds: Array<number | string> };
          this.options.onPresence?.(payload.onlineMemberIds.map(String));
        } else {
          this.options.onMessage(JSON.parse(frame.body) as BackendMessage);
        }
        return;
      }

      if (frame.command === 'ERROR') {
        this.options.onError();
      }
    });
  }

  private write(command: string, headers: Record<string, string>, body = '') {
    if (!this.socket || this.socket.readyState !== WebSocket.OPEN) return;
    const headerLines = Object.entries(headers).map(([key, value]) => `${key}:${value}`);
    this.socket.send(`${command}\n${headerLines.join('\n')}\n\n${body}\0`);
  }
}

function parseFrames(raw: string): StompFrame[] {
  return raw
    .split('\0')
    .map((frame) => frame.trim())
    .filter(Boolean)
    .map((frame) => {
      const [headerBlock, ...bodyParts] = frame.split('\n\n');
      const [command, ...headerLines] = headerBlock.split('\n');
      const headers = Object.fromEntries(
        headerLines
          .map((line) => line.split(':'))
          .filter(([key, value]) => key && value)
          .map(([key, ...value]) => [key, value.join(':')]),
      );

      return {
        command,
        headers,
        body: bodyParts.join('\n\n'),
      };
    });
}
```

- [ ] **Step 2: 컴파일 + 빌드 확인**

Run: `npm --prefix frontend run lint && npm --prefix frontend run build`
Expected: tsc 에러 없음, build 성공. (기존 채팅 동작 회귀 없음 — MESSAGE는 채팅 구독 id로 오면 그대로 onMessage.)

- [ ] **Step 3: 커밋**

```bash
git add frontend/src/lib/stomp.ts
git commit -m "refactor(stomp): 다중 구독 라우팅 + presence 구독 지원"
```

---

### Task 4: 프론트 App.tsx — presence 상태 배선

온라인 집합 state를 두고 `onPresence`로 갱신, ChatArea에 전달, 로그아웃 시 초기화.

**Files:**
- Modify: `frontend/src/App.tsx`

**Interfaces:**
- Consumes: `SpringStompClient` `onPresence` 옵션(Task 3).
- Produces: `ChatArea`에 `onlineMemberIds: Set<string>` prop 전달(Task 5가 소비).

- [ ] **Step 1: onlineMemberIds state 추가**

`const [presences, setPresences] = useState<Presence[]>([]);` 아래에 추가:
```ts
  const [onlineMemberIds, setOnlineMemberIds] = useState<Set<string>>(new Set());
```

- [ ] **Step 2: clearSession에서 초기화**

`clearSession` 안 `setPresences([]);` 아래에 추가:
```ts
    setOnlineMemberIds(new Set());
```

- [ ] **Step 3: STOMP 옵션에 onPresence 연결**

STOMP 이펙트의 `new SpringStompClient({ ... })`에서 `onMessage` 블록 뒤, `onDisconnect` 앞에 추가:
```ts
        onPresence: (ids) => {
          setOnlineMemberIds(new Set(ids));
        },
```

- [ ] **Step 4: ChatArea에 prop 전달**

`<ChatArea ... />`의 `onOpenProfile={...}` 아래(또는 적절한 위치)에 추가:
```tsx
              onlineMemberIds={onlineMemberIds}
```

- [ ] **Step 5: 컴파일 확인(이 시점엔 ChatArea가 아직 prop 미수용 → tsc 에러 예상)**

Run: `npm --prefix frontend run lint`
Expected: `ChatArea`에 `onlineMemberIds` prop 없음 에러 → **정상**(Task 5에서 해소). 커밋은 Task 5와 함께.

> App과 ChatArea는 prop 계약으로 묶여 있어 tsc가 둘을 함께 봐야 통과한다. 그래서 Task 4·5는 **한 커밋**으로 마무리한다(중간 커밋 없음).

---

### Task 5: 프론트 ChatArea.tsx — 온라인 점 + "온라인 N명"

참가자 명단에 온라인 표시를 추가한다.

**Files:**
- Modify: `frontend/src/components/ChatArea.tsx`

**Interfaces:**
- Consumes: `onlineMemberIds: Set<string>` prop(Task 4).

- [ ] **Step 1: props 인터페이스에 추가**

`ChatAreaProps`의 `onOpenProfile: (userId: string) => void;` 아래에 추가:
```ts
  onlineMemberIds: Set<string>;
```

- [ ] **Step 2: 구조분해에 추가**

컴포넌트 파라미터 구조분해에서 `onOpenProfile,` 아래에 추가:
```ts
  onlineMemberIds,
```

- [ ] **Step 3: 참가자 패널 헤더에 온라인 카운트 병기**

참가자 패널 헤더의 아래 블록을 교체:
```tsx
                <span className="text-xs font-bold text-text select-none">
                  참가자{participants ? ` ${participants.length}명` : ''}
                </span>
```
→
```tsx
                <span className="text-xs font-bold text-text select-none">
                  참가자{participants ? ` ${participants.length}명` : ''}
                  {participants
                    ? ` · 온라인 ${participants.filter((m) => onlineMemberIds.has(String(m.id))).length}명`
                    : ''}
                </span>
```

- [ ] **Step 4: 각 멤버 아바타에 온라인 점 표시**

참가자 li 안의 `<Avatar ... />`(참가자 명단 쪽, `member.profileImageUrl`을 쓰는 것)를 아래로 교체:
```tsx
                        <Avatar
                          photoUrl={member.profileImageUrl ?? undefined}
                          gradient={avatarForId(member.id)}
                          name={member.nickname}
                          className="w-8 h-8 rounded-lg text-xs flex-shrink-0"
                        />
```
→
```tsx
                        <div className="relative flex-shrink-0">
                          <Avatar
                            photoUrl={member.profileImageUrl ?? undefined}
                            gradient={avatarForId(member.id)}
                            name={member.nickname}
                            className="w-8 h-8 rounded-lg text-xs"
                          />
                          {onlineMemberIds.has(String(member.id)) && (
                            <span
                              className="absolute -bottom-0.5 -right-0.5 w-2.5 h-2.5 rounded-full bg-emerald-500 border-2 border-surface"
                              title="온라인"
                            />
                          )}
                        </div>
```

- [ ] **Step 5: 컴파일 + 빌드 확인(Task 4 변경 포함)**

Run: `npm --prefix frontend run lint && npm --prefix frontend run build`
Expected: tsc 에러 없음, build 성공.

- [ ] **Step 6: 커밋(Task 4 + 5 함께)**

```bash
git add frontend/src/App.tsx frontend/src/components/ChatArea.tsx
git commit -m "feat(presence): 참가자 명단 온라인 점 + 온라인 N명 표시"
```

---

### Task 6: 수동 E2E 검증 (두 탭 시나리오)

실제 온라인/오프라인 동작을 눈으로 확인한다. 프론트 유닛테스트가 없으므로 이게 진짜 행위 검증.

**전제:** 로컬 백엔드(8080) + Redis + 프론트 dev(5173) 기동. 로컬 계정 2개(예: `alice@test.com` / 시드 계정, 그리고 두 번째 계정). Redis는 `docker compose up -d redis`.

- [ ] **Step 1: 백엔드 + 프론트 기동**

```bash
# Redis
docker compose up -d redis
# 백엔드 (application-local 프로파일: ddl-auto update, 로컬 MySQL)
./gradlew bootRun
# 프론트 (다른 터미널)
npm --prefix frontend run dev
```

- [ ] **Step 2: 두 탭에서 계정 2개 로그인**

- 탭 A: 계정 A 로그인 → 채팅방 입장.
- 탭 B: 계정 B 로그인 → 같은(또는 다른) 방 입장 후 참가자 패널 열기.

- [ ] **Step 3: 온라인 표시 확인**

- 탭 B 참가자 패널: 계정 A에 **초록 온라인 점** + 헤더 "참가자 N명 · 온라인 M명"에서 M이 접속 인원과 일치.
- 브라우저 콘솔/네트워크(WS 프레임)에서 `/sub/presence`로 `{"onlineMemberIds":[...]}`가 오는지 확인.

- [ ] **Step 4: 오프라인 전이 확인**

- 탭 A를 닫음 → 몇 초 내 탭 B에서 계정 A 온라인 점 사라짐 + "온라인 M명" 감소.

- [ ] **Step 5: 멀티 탭 확인**

- 계정 A로 탭 2개 열고 하나만 닫음 → 여전히 온라인(마지막 세션까지 살아있어야 오프라인).

- [ ] **Step 6: 관찰 결과 기록**

- 모두 통과하면 Task 완료. 이상 있으면 해당 Task로 돌아가 수정.

---

## Self-Review 결과

- **스펙 커버리지:** 온라인 추적(Task 1) / 세션 이벤트 방송·late-join 동기화(Task 2) / 프론트 다중 구독(Task 3) / 상태 배선(Task 4) / UI 온라인 점·카운트(Task 5) / E2E(Task 6) — 스펙 전 항목 매핑됨. 비목표(타이핑·방 위치·Redis)는 의도적 제외.
- **Placeholder:** 없음(모든 스텝 실제 코드/명령 포함).
- **타입 일관성:** `onlineMemberIds`(Set<string>) · `onPresence(string[])` · 백엔드 `onlineMemberIds: List<Long>` → 프론트 `.map(String)` 변환 일관. `String(member.id)` 비교 일관.
